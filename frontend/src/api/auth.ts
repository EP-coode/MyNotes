import { AuthResponse } from "./interfaces/auth";

async function login(
  email: string,
  password: string
): Promise<AuthResponse | { error: string }> {
  
}


async function refresh(params:type) {
  
}

async function logout(params:type) {
  
}

async function register(params:type) {
  
}